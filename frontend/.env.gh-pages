# KasirGo Frontend — GH Pages build environment
# Vite loads .env + .env.gh-pages when --mode gh-pages
VITE_DEV_MODE=true

# Set ke Railway URL setelah backend di-deploy
# Contoh: VITE_API_BASE=https://kasirgo-production.up.railway.app
VITE_API_BASE=
