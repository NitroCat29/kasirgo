{
  "name": "kasirgo",
  "version": "1.0.0",
  "description": "KasirGo - POS System with Zig WASM",
  "type": "module",
  "scripts": {
    "build": "cd frontend && bun run build",
    "dev": "concurrently -n backend,frontend -c blue,green \"bun --hot backend/server.ts\" \"cd frontend && bun run dev\"",
    "dev:frontend": "cd frontend && bun run dev",
    "build:frontend": "cd frontend && bun run build"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
